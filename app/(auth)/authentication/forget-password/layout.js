export const metadata = {
  title: "Reset password",
  description: "Reset access to your CommitDiary account.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ForgetPasswordLayout({ children }) {
  return children;
}
