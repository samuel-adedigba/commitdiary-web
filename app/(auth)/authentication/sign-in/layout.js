export const metadata = {
  title: "Sign in",
  description: "Sign in to your CommitDiary developer work journal.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SignInLayout({ children }) {
  return children;
}
