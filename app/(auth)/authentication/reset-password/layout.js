export const metadata = {
  title: "Choose a new password",
  description: "Choose a new password for your CommitDiary account.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
