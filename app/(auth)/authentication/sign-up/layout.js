export const metadata = {
  title: "Create account",
  description: "Create a CommitDiary developer work journal account.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SignUpLayout({ children }) {
  return children;
}
